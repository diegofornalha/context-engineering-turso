{
    "compilerOptions": {
        /* Visite https://aka.ms/tsconfig.json para ler mais sobre este arquivo */

        /* Define a versão da linguagem JavaScript para o JavaScript emitido e inclui declarações de biblioteca compatíveis. */
        "target": "es2021",
        /* Especifica um conjunto de arquivos de declaração de biblioteca empacotados que descrevem o ambiente de tempo de execução alvo. */
        "lib": ["es2021"],
        /* Especifica qual código JSX é gerado. */
        "jsx": "react-jsx",

        /* Especifica qual código de módulo é gerado. */
        "module": "es2022",
        /* Especifica como o TypeScript procura um arquivo a partir de um especificador de módulo fornecido. */
        "moduleResolution": "bundler",
        /* Especifica nomes de pacotes de tipo a serem incluídos sem serem referenciados em um arquivo de origem. */
        "types": [
            "./worker-configuration.d.ts",
            "node"
        ],
        /* Habilita a importação de arquivos .json */
        "resolveJsonModule": true,

        /* Permite que arquivos JavaScript façam parte do seu programa. Use a opção `checkJS` para obter erros desses arquivos. */
        "allowJs": true,
        /* Habilita o relatório de erros em arquivos JavaScript com verificação de tipo. */
        "checkJs": false,

        /* Desabilita a emissão de arquivos de uma compilação. */
        "noEmit": true,

        /* Garante que cada arquivo possa ser transpilado com segurança sem depender de outras importações. */
        "isolatedModules": true,
        /* Permite 'import x from y' quando um módulo não tem uma exportação padrão. */
        "allowSyntheticDefaultImports": true,
        /* Garante que a capitalização esteja correta nas importações. */
        "forceConsistentCasingInFileNames": true,

        /* Habilita todas as opções estritas de verificação de tipo. */
        "strict": true,

        /* Pula a verificação de tipo de todos os arquivos .d.ts. */
        "skipLibCheck": true
    },
    "exclude": ["example/**/*"]
}